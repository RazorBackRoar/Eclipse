{
  "id": "{{ item.id }}",
  "name": "{{ item.name }}",
  "transport": "{{ data.get("transport", "stdio") }}",
  "command": "{{ data.get("command", "") }}",
  "args": {{ data.get("args", []) | tojson }},
  "env": {{ data.get("env", {}) | tojson(indent=2) }}
}
